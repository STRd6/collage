app
  tools
    button(click=@render) Render
    
  workspace
    scene(drop=@workspaceDrop dragover=@workspaceDragover @mousedown @mousemove @mouseup)
    = @screenElement
  materials(dragstart=@materialDrag)
    = @images
