- Tool = require "../presenters/tool"
- editor = this

app
  tools
    - @tools.forEach (tool) ->
      = Tool tool, editor
  workspace
    scene(@drop @dragover @mousedown @mousemove)
    = @screenElement
  tray
    materials(@dragstart)
      = @images
    actions
      button(click=@render) Render
